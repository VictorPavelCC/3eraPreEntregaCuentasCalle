<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <h1 class="navbar-brand fs-3 p-0">Resumen</h1>
      <ul class="navbar-nav ml-auto">
        <li class="nav-item d-flex align-items-center">
          <a class="nav-link d-flex p-0 fs-5 align-items-center" href="/api/sessions/"><i
              class="bi bi-person-circle ms-3"></i>
          </a>
        </li>
      </ul>
    </div>
  </nav>
  <div class="d-flex justify-content-center align-items-center" style="height: 100vh;">
    <div class="info-compra text-center">
      <div class="ticket mb-4">
        <h2>Código de Compra: {{ticket.code}}</h2>
        <p>Comprador: {{ticket.purchaser}}</p>
        <p>Fecha de la compra: {{ticket.date}}</p>
        <p>Tienda: 127.0.0.1</p>
      </div>

      <h3>Productos Comprados:</h3>
      <ul class="list-unstyled">
        {{#each purchasedProducts}}
        <li>{{this.productName}} - Cantidad solicitada: {{this.quantity}} - ${{this.totalPrice}}</li>
        {{/each}}
      </ul>

      {{#if notPurchasedProducts.length}}
      <h3>Productos No Comprados:</h3>
      <ul class="list-unstyled">
        {{#each notPurchasedProducts}}
        <li>{{this.productName}} - Cantidad solicitada: {{this.quantity}} - ${{this.totalPrice}}</li>
        {{/each}}
      </ul>
      {{/if}}

      <div class="bg-success text-white p-2 mt-4">Total de la compra: {{ticket.amount}}</div>

      {{#if notPurchasedProducts.length}}
      <img src="https://external-preview.redd.it/k_j6UTRkmPbizti-eat964aUMclgpfsfnHCgjzUaywo.png?auto=webp&s=f1183c7531df95464c336e54128f7d48f2462de4" alt="NOT_OK">
      {{else}}
      <img src="https://static.wikia.nocookie.net/fallout/images/1/18/Icon_Fo4_side_quest.png" alt="OK">
      {{/if}}
      </ul>



    </div>
  </div>